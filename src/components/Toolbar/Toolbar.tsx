import * as React from "react";

import { SettingsButton } from "..";

import "./styles.scss";

const Toolbar: React.FC = () => {
  return (
    <div>
      <SettingsButton />
    </div>
  );
};

export default Toolbar;
